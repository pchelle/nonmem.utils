# tooltip_text

When interactive plots are generated using `plotly`, `tootltip` will
display text aesthetic allowing easy tracking of data.

## Usage

``` r
tooltip_text(data, var_names)
```

## Arguments

- data:

  A data.frame of data

- var_names:

  A vector of variable names to display
